# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
.pytest_cache/

# Virtual environments
.venv

# Secrets
.env

# Local recon / downloaded data (may contain customer PII)
backend/recon/
backend/downloads/

# Runtime state (which massive-export job we're tracking, last extraction run)
# -- machine/run specific, not something to version.
backend/state/

# frontend/ has its own .gitignore (node_modules, .output, .nitro, .tanstack,
# etc.) generated by the TanStack CLI scaffold -- nothing to add here.
